## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already in repo (Button, Dialog, Input, Textarea, Select, Badge, Separator, Toaster)
SEO handled via document.title + meta[name="description"] updates per route (no react-helmet dependency)
All fetches use credentials: "include"
