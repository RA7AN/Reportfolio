# Phase 8 — Production review **[deferred]**

PRD: §42–43, §52 Phase 8.

**When:** only after explicit user approval.

- Merge redesign branch via PR (CI `verify` green).
- Do not push `main` directly, do not change Vercel settings unless asked.
- Then verify production routes and `SITE_URL`.
